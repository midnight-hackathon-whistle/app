import 'package:flutter/material.dart';

class MembershipCertificateScreen extends StatelessWidget {
  const MembershipCertificateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Membership Certificate'),
      ),
      body: const Center(
        child: Text('Membership Certificate Screen'),
      ),
    );
  }
}
